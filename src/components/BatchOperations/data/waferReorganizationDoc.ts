// src/data/waferReorganizationDoc.ts

export const waferReorganizationDocumentation = `
\`WaferBasketReorganizationModule.tsx\` 组件是一个用于晶圆片在不同片篮之间进行重组和转移的核心模块。它提供了一个直观的界面，允许用户选择源片篮中的晶圆片，并根据不同的转移模式将其移动到目标片篮中。

### 功能概述

该组件主要用于处理晶圆片的物理移动和逻辑归属变更，例如在拆批、并批、片篮更换等操作中。它将晶圆片从一个或多个源片篮中取出，并放置到一个或多个目标片篮中，同时支持对晶圆片类型（良品、不良品、损失）的标记和槽位顺序的调整。

### 主要功能点

1.  **源片篮列表 (Source Carriers List)**
    *   **显示**: 展示所有可用的源片篮（Carrier ID、Sublot ID、良品数、不良品数）。
    *   **选择**: 用户可以点击列表中的片篮来选择当前操作的源片篮，选中的片篮会高亮显示。
    *   **数据来源**: 通过 \`initialSourceCarriers\` 属性接收外部传入的源片篮数据。

2.  **源晶圆列表 (Source Wafers List)**
    *   **显示**: 展示当前选中源片篮中的所有晶圆片，包括槽位号 (Slot No)、类型 (Type) 和晶圆 ID (Wafer ID)。槽位号按 25->1 降序显示。
    *   **晶圆类型**: 每个晶圆片都有一个下拉选择框，允许用户手动更改晶圆类型（GOOD, BAD, LOSS）。\`disableWaferTypeSelection\` 属性可以禁用此功能。
    *   **晶圆选择**: 用户可以通过复选框选择一个或多个晶圆片，以便进行批量操作或转移。
    *   **批量操作**:
        *   提供“批量良好”、“批量不良”、“批量Loss”按钮，允许用户对选中的晶圆片批量设置类型。
        *   \`showBatchActionButtons\` 属性控制这些按钮的显示。
        *   \`disableBatchWaferTypeActions\` 属性可以禁用这些批量操作按钮。

3.  **转移操作区域 (Transfer Controls)**
    *   **左右箭头按钮**:
        *   **右箭头 (\`ChevronRight\`)**: 将选中的源晶圆片移动到当前选中的目标片篮中。
        *   **左箭头 (\`ChevronLeft\`)**: 预留功能，目前提示“向左移动功能待实现”。
        *   \`hideTransferButtons\` 属性可以隐藏这些按钮。
    *   **转移模式选择 (\`transferMode\`)**:
        *   **平移 (默认)**: 源晶圆按从上到下（25->1）的顺序取出，填充到目标片篮的空槽位中也按从上到下（25->1）的顺序。
        *   **1>25**: 源晶圆按从下到上（1->25）的顺序取出，填充到目标片篮的空槽位中按从上到下（25->1）的顺序。
        *   **25>1**: 源晶圆按从上到下（25->1）的顺序取出，填充到目标片篮的空槽位中按从下到上（1->25）的顺序。
        *   这些模式决定了晶圆从源片篮中被“挑选”的顺序，以及它们被“放置”到目标片篮空槽位中的顺序。

4.  **目标片篮列表 (Target Carriers List)**
    *   **显示**: 展示所有已创建的目标片篮（片篮 ID、良品数、不良品数）。
    *   **选择**: 用户可以点击列表中的片篮来选择当前操作的目标片篮。
    *   **添加/删除**:
        *   “增加片篮”按钮允许用户添加新的目标片篮，并自动生成一个默认 ID。
        *   每个目标片篮旁边有“删除”按钮，允许用户移除片篮及其中的所有晶圆。
    *   **ID 编辑**: 允许用户修改目标片篮的 ID，但会进行重复性检查。
    *   **移动按钮**: 预留“上移”、“下移”按钮，用于调整目标片篮的顺序。\`hideTargetMoveButtons\` 属性可以隐藏这些按钮。

5.  **目标晶圆列表 (Target Wafers List)**
    *   **显示**: 展示当前选中目标片篮中的所有晶圆片，包括槽位号 (Slot No)、类型 (Type) 和晶圆 ID (Wafer ID)。槽位号按 25->1 降序显示。
    *   **空槽位**: 目标片篮始终显示 25 个槽位，未被晶圆填充的槽位会显示为空。
    *   **晶圆类型**: 每个晶圆片都有一个下拉选择框，允许用户手动更改晶圆类型。
    *   **槽位调整**: 每个晶圆片旁边有“上移”、“下移”按钮，允许用户在当前片篮内调整晶圆的槽位顺序。

### 属性 (Props)

*   \`initialSourceCarriers\`: \`CarrierData[]\` - 初始的源片篮数据数组。
*   \`selectedMode\`: \`string\` - 当前的倒篮模式，影响组件的初始化行为和某些操作的可用性（例如模式 A, B, C, D 会使用源片篮作为目标片篮，模式 3 会禁用目标片篮）。
*   \`onReorganizationStateChange\`: \`(finalTargetCarriers: TargetCarrier[], finalSourceWafers: WaferData[]) => void\` - 当目标片篮或晶圆状态发生变化时触发的回调函数，用于将最新的重组状态传递给父组件。
*   \`disableWaferTypeSelection\`: \`boolean\` (可选) - 如果为 \`true\`，则禁用源晶圆列表中的晶圆类型选择下拉框。
*   \`showBatchActionButtons\`: \`boolean\` (可选, 默认 \`true\`) - 如果为 \`false\`，则隐藏源晶圆列表上方的批量操作按钮。
*   \`hideTargetMoveButtons\`: \`boolean\` (可选, 默认 \`false\`) - 如果为 \`true\`，则隐藏目标片篮列表上方的“上移”、“下移”按钮。
*   \`hideTransferButtons\`: \`boolean\` (可选, 默认 \`false\`) - 如果为 \`true\`，则隐藏源晶圆列表和目标片篮列表之间的左右箭头转移按钮。
*   \`disableBatchWaferTypeActions\`: \`boolean\` (可选, 默认 \`false\`) - 如果为 \`true\`，则禁用源晶圆列表上方的批量设置晶圆类型按钮。

### 内部状态 (State)

*   \`sourceCarriers\`: 当前显示的源片篮列表。
*   \`targetWafers\`: 包含所有目标片篮中晶圆（包括空槽位）的扁平化列表。
*   \`selectedWafers\`: 当前在源晶圆列表中被选中的晶圆 ID 数组。
*   \`selectedSourceCarrierId\`: 当前选中的源片篮 ID。
*   \`displayedSourceWafers\`: 当前选中源片篮中显示的晶圆列表。
*   \`targetCarriersList\`: 当前创建的所有目标片篮列表。
*   \`selectedTargetCarrierId\`: 当前选中的目标片篮 ID。
*   \`transferMode\`: 当前选择的晶圆转移模式（"平移", "1>25", "25>1"）。

### 交互逻辑

1.  用户从左侧的“源片篮”列表中选择一个片篮。
2.  中间的“源片篮晶圆列表”会显示该片篮中的所有晶圆。
3.  用户可以在“源片篮晶圆列表”中选择一个或多个晶圆，并可选择批量修改其类型。
4.  用户选择一个“转移模式”（平移、1>25、25>1），这决定了晶圆从源片篮中被“挑选”的顺序和填充到目标片篮空槽位中的顺序。
5.  用户可以添加新的“目标片篮”，并从右侧的“目标片篮”列表中选择一个目标片篮。
6.  点击右箭头按钮，选中的晶圆将根据选定的转移模式移动到目标片篮的空槽位中。
7.  目标片篮中的晶圆可以手动调整类型和槽位顺序。
8.  \`onReorganizationStateChange\` 回调函数会实时更新父组件关于目标片篮和晶圆的最新状态。
`;
